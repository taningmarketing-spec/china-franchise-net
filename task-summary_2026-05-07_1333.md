# 中国国际加盟网 - 图片上传功能

## 时间
2026-05-07 13:33

## 需求
后台品牌编辑页面的"图片设置"区域，除了输入 URL 外，还需要支持**上传图片**（主图 Banner + Logo）。

## 实现内容

### 1. 新建图片上传 API
**文件**: `app/api/upload/route.ts`
- POST `/api/upload` 接收 multipart/form-data 文件
- 支持格式：JPG/PNG/GIF/WebP/SVG
- 文件大小限制：5MB
- 自动生成唯一文件名（时间戳+随机字符串）
- 存储到 `public/uploads/` 目录
- 返回可访问的 URL 路径 `/uploads/xxx.jpg`

### 2. 品牌编辑页面改造
**文件**: `app/admin/brands/[id]/page.tsx`

每个图片字段（Banner / Logo）现在都有：
- **URL 输入框** — 可手动粘贴外部链接
- **「上传图片」按钮** — 点击选择本地文件上传
- **实时预览图** — 上传/输入 URL 后立即显示缩略图
- **删除按钮 (✕)** — 清除已选图片
- **上传状态** — 上传中显示 loading 动画 + 禁用按钮

交互流程：
1. 用户点击"上传图片" → 触发隐藏 `<input type="file">`
2. 选择文件 → 自动调用 `/api/upload` 上传
3. 上传成功 → 自动填充 URL 输入框 + 显示预览图
4. 提交表单时 URL 一并提交保存

### UI 细节
- Banner 上传按钮：蓝色 (`bg-blue-50 text-blue-600`)
- Logo 上传按钮：绿色 (`bg-emerald-50 text-emerald-600`)
- 按钮带上传图标（箭头向上）
- 预览图带圆角边框和右上角关闭按钮

## 服务器状态
✅ localhost:3000 编译通过，无错误