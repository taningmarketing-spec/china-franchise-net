# 出海服务详情页优势列表后台编辑完成

## 任务
详情页右侧5项优势列表从硬编码改为数据库可编辑（多语言支持）。

## 完成内容

### 1. API 路由更新 ✅
- `app/api/overseas-services/route.ts` (POST) - 添加 advantages/advantagesEn/advantagesTh/advantagesVi 字段
- `app/api/overseas-services/[id]/route.ts` (PUT) - 添加 advantages 相关字段

### 2. 后台编辑页 `app/admin/overseas-services/[id]/page.tsx` ✅
- FormData 添加 `advantages/advantagesEn/advantagesTh/advantagesVi` 字段
- 添加 `AdvantageItem` 接口（icon + text）
- `parseAdvantages()` JSON 解析辅助函数
- `ADVANTAGE_ICONS` emoji 选择列表（10个备选）
- `advantageItems` state 状态管理
- `useEffect` 同步当前语言 advantages → advantageItems
- CRUD 函数：`updateAdvantageText`、`updateAdvantageIcon`、`addAdvantage`、`removeAdvantage`
- UI 新增"优势列表"编辑区块：每项一个 emoji 下拉选择器 + 文本输入框 + 删除按钮，最多5项

### 3. 前台 `ServiceDetailClient.tsx` ✅
- 删除硬编码的5项 advantages 列表
- 新增 `parseAdvantages()` 解析函数
- 新增 `getAdvantages()` 多语言读取函数（优先读对应语言，回退中文）
- 渲染从数据库读取的优势列表，无内容时显示提示

## 数据库字段
- `advantages` - 中文优势 JSON 数组 `[{icon: "💰", text: "..."}]`
- `advantagesEn/advantagesTh/advantagesVi` - 各语言版本

## 验证
- Dev server 已重启
- 后台编辑页可编辑每项优势的图标和文案（按语言切换）
- 前台详情页从数据库读取优势列表显示

## 项目状态
- 域名：cnfranchise.com
- Dev server：localhost:3000
- 数据库：prisma/dev.db（SQLite，schema 已更新）
- 出海服务：10 条记录
