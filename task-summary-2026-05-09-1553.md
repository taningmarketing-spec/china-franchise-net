# 出海服务 V2 升级 - 完成

## 目标
将出海服务页面从 emoji 图标改为真实图片，点击"马上咨询"跳转到详情页。

## 关键实现

### 1. 统一数据文件
**文件**: `data/overseas-services.ts`
- 10大服务完整数据（多语言：zh/en/th/vi）
- 每个服务含：icon（图片URL）、name、shortDesc、heroTitle、heroSubtitle、features、process、whyChoose
- 导出 `getServiceById()` 和 `serviceIds` 工具函数
- 图片使用 Unsplash 高质量图片（专业图库，无版权问题）

### 2. 列表页改造
**文件**: `app/[locale]/overseas-services/OverseasServicesClient.tsx`
- emoji → Unsplash 图片（Image 组件，4:3 比例）
- 卡片整体可点击跳转详情页
- 两个按钮："了解更多"（主色）+ "马上咨询"（边框按钮）
- 悬停动效：图片放大 + 卡片上浮 + 阴影加深

### 3. 详情页
**文件**: 
- `app/[locale]/overseas-services/[serviceId]/page.tsx` - 服务端页面，generateStaticParams + generateMetadata
- `app/[locale]/overseas-services/[serviceId]/ServiceDetailClient.tsx` - 详情页客户端组件

**详情页结构**:
- 面包屑导航（返回列表）
- 全屏 Hero 图（服务大图 + 渐变遮罩 + 标题）
- 服务内容（2列网格，勾选图标）
- 服务流程（5步骤横向时间轴）
- 为什么选我们（星星图标，2列）
- 侧边栏（固定）：CTA 卡片 + 其他服务推荐
- 相关服务（底部4个推荐卡片）

### 4. 技术细节
- `next.config.mjs` 已有 `remotePatterns: [{ protocol: 'https', hostname: '**' }]`，无需额外配置
- 所有页面返回 HTTP 200，4语言全部正常
- Next.js Image 组件 + `unoptimized` 标志（避免图片优化导致的 Unsplash URL 问题）

## 验证结果
- `/zh/overseas-services` → 200
- `/en/overseas-services` → 200
- `/th/overseas-services` → 200
- `/vi/overseas-services` → 200
- `/zh/overseas-services/legal` → 200
- `/zh/overseas-services/ip` → 200
- `/en/overseas-services/logistics` → 200

## 待后续完善（不在本次范围内）
- 后台管理系统上传图片替换占位图
- API 路由接入数据库（sql.js 已就绪，但 Prisma Client 被锁定）
- ServiceInquiryModal.tsx 保留但不再使用（可后续删除）
