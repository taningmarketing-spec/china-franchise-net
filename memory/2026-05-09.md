# 2026-05-09 工作日志

## 学院模块优化

### 已完成

1. **文章封面图片更新** - 140篇文章全部更新为品牌专属图片
2. **占位符内容填充** - 139/140篇文章填充真实HTML内容
3. **摘要清理** - 移除65篇文章的模板摘要文字
4. **学院列表页改进**：
   - 添加头图显示（featuredImg 字段）
   - 分页组件（页码按钮 + 每页条数选择器）
   - 分类图标 fallback
5. **学院首页 Featured 区域** - 从硬编码改为读取最新3篇真实文章
6. **运行时错误修复**：
   - `/zh/academy/page.tsx` 移除服务端组件中的 `onError` 事件处理器
   - 清理 `.next` 缓存修复 404 问题

### 修复记录

**问题**: `/zh/academy` 返回 404
**根因**: Next.js webpack 缓存损坏（Fast Refresh runtime error 导致）
**解决**: 删除 `.next` 目录，重启 dev server

**问题**: "Event handlers cannot be passed to Client Component props"
**根因**: 服务端组件 `<img>` 标签使用了 `onError` 事件处理器
**解决**: 移除 `onError` 属性

### 待处理

- `zh / taning-thailand-deep-dive-zh` 文章仍有占位符内容

### 验证状态

- `/zh/academy` ✅ 200
- `/zh/academy/overseas-dynamic` ✅ 200
- `/admin/dashboard` ✅ 200
